{% macro genEntry(limit, size) %}
  {% if limit == size %}
      <li class="active"><span>{{ size }}</span></li>
  {% else %}
      <li><a href="{{ mergePath({'limit': size}, true) }}">{{ size }}</a></li>
  {% endif %}
{% endmacro %}
{% import _self as list %}

<div class="limit">
  <ul class="pagination">
    <li class="limit-description disabled"><span>{% trans %}Limit{% endtrans %}</span></li>
    {{ list.genEntry(limit, 10) }}
    {{ list.genEntry(limit, 25) }}
    {{ list.genEntry(limit, 50) }}
    {{ list.genEntry(limit, 100) }}
    {{ list.genEntry(limit, 250) }}
    {{ list.genEntry(limit, 1000) }}
  </ul>
</div>
